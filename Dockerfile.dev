FROM ruby:2.4

WORKDIR /usr/src

COPY ./jekyll /usr/src
COPY ./.git /usr/src/.git

RUN bundle install

EXPOSE 80

CMD ["jekyll", "serve", "--watch", "--port", "80", "--host", "0.0.0.0", "--future", "true"]
