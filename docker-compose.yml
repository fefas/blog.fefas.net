version: "3"

services:
    jekyll:
        build: ./
        image: fefas/fefas.net
        ports:
            - 80:80
        volumes:
            - ./.git:/usr/src/.git
            - ./_data:/usr/src/_data
            - ./_layouts:/usr/src/_layouts
            - ./_plugins:/usr/src/_plugins
            - ./_posts:/usr/src/_posts
            - ./assets:/usr/src/assets
            - ./pages:/usr/src/pages
